# 개발 프로파일 — SQL 로그 노출, 디버그 로깅
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
    re.kr.icuh.drought.openapi: debug
